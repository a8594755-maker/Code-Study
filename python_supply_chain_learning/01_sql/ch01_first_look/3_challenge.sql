-- ch01 第 3 關：白紙挑戰
-- 通過這一關 ＝ ch01 的概念在概念表升級為 Independent（獨立完成）。
-- 規則：
--   1. 不看 1_lesson.sql、不看 2_practice.sql，直接在作答區寫。
--   2. 卡住可以請家教給提示，但不能直接要完整答案。
--   3. 題目用全新的參數與問題，照抄行不通——但這是「第三次寫」，你已經會了。
-- 執行方式：一次只選一段 SQL（選到分號 ; 為止），按 Cmd + Shift + E。

-- ============================================================
-- 挑戰 1：品類翻譯表長什麼樣？
-- ============================================================
-- 商業情境：行銷同事問你——「聽說有一張表把葡萄牙文的商品品類名
-- 翻成英文？我想看看它長什麼樣。」
-- 任務：預覽 olist.product_category_translation_raw 的全部欄位，
-- 只看 12 筆。
--
-- 你的作答區：




-- 我的觀察：這張表有 ______ 個欄位，一列代表 ______________________

-- ============================================================
-- 挑戰 2：訂單明細有多少？運費最貴的是哪幾筆？
-- ============================================================
-- 商業情境：物流主管問你兩件事——
-- 任務 2a：olist.order_items_raw 一共有幾筆明細？
--          結果欄取名 item_row_count。
-- 任務 2b：從 order_items_raw 選出 order_id、product_id、freight_value
--          三欄，按運費（freight_value）由高到低排序，只看 5 筆。
--
-- 你的作答區（兩段查詢分開寫，各自以分號結尾）：




-- 我的觀察：最高的一筆運費大約是 ______ 巴西幣

-- （往下捲之前，先把上面兩題寫完、跑通、填好觀察。）
--
--
--
--
--
--
--
--
--
--
--
--
--
--
--
-- ============================================================
-- === 做完前不要偷看 ===
-- ============================================================
-- 挑戰 1 參考答案：
--
--   SELECT *
--   FROM olist.product_category_translation_raw
--   LIMIT 12;
--
-- 挑戰 2a 參考答案：
--
--   SELECT COUNT(*) AS item_row_count
--   FROM olist.order_items_raw;
--
--   （COUNT(*)＝計算列數；預期約 112,650 筆。）
--
-- 挑戰 2b 參考答案：
--
--   SELECT
--       order_id,
--       product_id,
--       freight_value
--   FROM olist.order_items_raw
--   ORDER BY freight_value DESC
--   LIMIT 5;
--
--   （ORDER BY＝排序；DESC＝遞減，由大到小。）
--
-- 對答案的原則：欄位順序或別名略有不同沒關係，重點是
-- 結果正確、而且你能解釋每一行在做什麼。

-- ============================================================
-- 理解關卡（過關才建 ch02；用自己的話回答，中文即可，家教記入 PROGRESS.md）
-- ============================================================
--
-- 關卡 1：不看任何檔案，說出（或打出）olist 這個 schema（資料表分組）
--         裡的表，並現場預覽其中一張（自選）。
--
-- 關卡 2：用 Excel 活頁簿的類比，向家教解釋這四個詞分別是什麼：
--         schema／table（資料表）／row（資料列）／column（欄位）。
--
-- 兩關都通過 → 家教更新概念表 → 當天建立 ch02_filter_sort/。
