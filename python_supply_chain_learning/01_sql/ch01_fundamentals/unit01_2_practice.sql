-- Chapter 1 / Unit 1 練習：換你動手（這是你的檔案）
-- 規則：
--   家教一次只帶你做 1 題；目前題目完成、執行、review 後才進下一題。
--   練習 1–3 附答案：先讀懂 → 遮住 → 憑記憶重打一遍。
--   練習 4–7 給查詢骨架：把 ______ 填上正確的關鍵字或名稱。
--   練習 8–10 只給商業需求：全部自己寫。
-- 執行方式：一次只選一段 SQL（選到分號 ; 為止），Windows 按 Ctrl + Shift + E；macOS 按 Cmd + Shift + E。
-- 注意：題目刻意用和 unit01_1_lesson.sql 不同的欄位、數字（大多也換了表）——照抄行不通。

-- ============================================================
-- 暖身區（每次上課先做，永不跳過）
-- ============================================================
-- 家教每次上課會口頭出 3 題（1 題上次教的、1 題上週的、1 題更久以前的），
-- 你「憑記憶」把答案打在下面的空白處，打完才開始今天的進度。
-- 答錯完全沒關係——答錯的題目只是會早一點再出現一次。




-- ============================================================
-- 練習 1：平台上有幾個賣家？（附答案）
-- ============================================================
-- 分析師的問題：sellers_raw 一共有幾筆賣家資料？
-- 為什麼：接手新表的第一件事就是先知道它多大。
-- 答案：

SELECT COUNT(*) AS seller_row_count
FROM olist.sellers_raw;

-- COUNT(*)（計算列數）數出全表筆數；AS（別名）幫結果欄取好讀的名字。
-- 你的任務：跑一次看結果，然後遮住答案，在下面憑記憶重打一遍。



-- 我的執行結果：sellers_raw 共 __________ 筆

-- ============================================================
-- 練習 2：商品表的一列長什麼樣？（附答案）
-- ============================================================
-- 分析師的問題：products_raw 記了商品的哪些資訊？
-- 為什麼：SELECT（選取欄位）* 加 LIMIT（限制列數）＝ 安全的第一眼。
-- 答案：

SELECT *
FROM olist.products_raw
LIMIT 5;

-- FROM（資料來源）指定要讀哪張表；LIMIT 5 只拿前 5 列，不驚動整張表。
-- 你的任務：跑一次看結果，然後遮住答案，在下面憑記憶重打一遍。



-- 我的執行結果：products_raw 用哪一欄當商品編號？__________

-- ============================================================
-- 練習 3：客戶清單只留三欄（附答案）
-- ============================================================
-- 分析師的問題：想快速掃一眼客戶分布，只需要編號、城市、州。
-- 為什麼：點名欄位的結果比 SELECT * 好讀——像 Excel 只留必要欄。
-- 答案：

SELECT
    customer_id,
    customer_city,
    customer_state
FROM olist.customers_raw
LIMIT 15;

-- 你的任務：跑一次看結果，然後遮住答案，在下面憑記憶重打一遍。



-- 我的執行結果：出現最多次的州看起來是 __________

-- ============================================================
-- 練習 4：付款表有幾筆資料？（填空）
-- ============================================================
-- 分析師的問題：order_payments_raw 一共幾筆？
-- 把 ______ 填上正確的字，寫成完整查詢再執行：
--
--   SELECT ______(*) AS payment_row_count
--   ______ olist.order_payments_raw;
--
-- 你的作答區：



-- ============================================================
-- 練習 5：預覽評論表（填空）
-- ============================================================
-- 分析師的問題：order_reviews_raw 的一列長什麼樣？只看 8 筆。
--
--   ______ *
--   FROM olist.______
--   ______ 8;
--
-- 你的作答區：



-- ============================================================
-- 練習 6：付款清單只留三欄（填空）
-- ============================================================
-- 分析師的問題：每筆付款的訂單編號、付款方式、金額，只看 12 筆。
--
--   SELECT
--       order_id,
--       ______,
--       payment_value
--   FROM olist.order_payments_raw
--   ______ 12;
--
-- 你的作答區：



-- ============================================================
-- 練習 7：最貴的商品明細（填空）
-- ============================================================
-- 分析師的問題：order_items_raw 裡單價最高的前 7 筆明細？
-- 提示：ORDER BY（排序）＋ DESC（遞減，由大到小）。
--
--   SELECT
--       order_id,
--       product_id,
--       price
--   FROM olist.order_items_raw
--   ORDER ______ price ______
--   LIMIT 7;
--
-- 你的作答區：



-- ============================================================
-- 練習 8：評論總數（只給需求）
-- ============================================================
-- 商業需求：客服主管想知道 order_reviews_raw 一共有幾筆評論，
-- 結果欄請取名 review_row_count。
--
-- 你的作答區：



-- ============================================================
-- 練習 9：看一眼地理表（只給需求）
-- ============================================================
-- 商業需求：老闆聽說 geolocation_raw 超級大，他只想「看一眼」——
-- 預覽全部欄位，只要 6 筆。
-- （先想一下：這張表約 100 萬列，哪個關鍵字絕對不能漏？）
--
-- 你的作答區：



-- ============================================================
-- 練習 10：最新的 10 筆評論（只給需求）
-- ============================================================
-- 商業需求：品質團隊想看最新的評論。從 order_reviews_raw 選出
-- review_id、order_id、review_score、review_creation_date 四欄，
-- 最新建立的評論排最前面，只看 10 筆。
--
-- 你的作答區：



-- ============================================================
-- 全部完成後 → 打開 unit01_3_challenge.sql 進行白紙挑戰。
-- 提醒：這個檔案的所有作答區只能由你本人填寫，家教永不代填此檔。
-- ============================================================
